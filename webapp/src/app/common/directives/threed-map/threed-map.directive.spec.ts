import { ThreedMapDirective } from '../threed-map.directive';

describe('ThreedMapDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreedMapDirective();
    expect(directive).toBeTruthy();
  });
});
